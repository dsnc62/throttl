{
	"name": "throttl",
	"version": "1.0.0",
	"description": "EECS4413 Project",
	"scripts": {
		"build": "pnpm run -r build",
		"dev": "pnpm run -r --parallel --aggregate-output dev",
		"dev:db": "turso dev --db-file throttl.db",
		"start": "pnpm run -r --parallel --aggregate-output start",
		"format": "biome format",
		"lint": "biome lint",
		"check": "biome check"
	},
	"devDependencies": {
		"@biomejs/biome": "catalog:"
	}
}
